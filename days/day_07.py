from dataclasses import dataclass


@dataclass
class Day07:
    lines: list[str]

    def part_one(self) -> int:
        score = 0
        return score

    def part_two(self) -> int:
        score = 0
        return score
